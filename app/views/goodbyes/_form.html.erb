<%= form_for(@goodbye) do |f| %>
  <% if @goodbye.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@goodbye.errors.count, "error") %> prohibited this goodbye from being saved:</h2>

      <ul>
      <% @goodbye.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
