<%=form_for @improve,:html=>{:class=>'slboard_form'}  do |f| %>
	<% if @improve.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@improve.errors.count, "error") %> prohibited this improve from being saved:</h2>
      <ul>
      <% @improve.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :title,'제목' %><br />
    <%= f.text_field :title,:class=>'sl_input',:required=>'required' %>
  </div>
  <div class="field">
    <%= f.label :content,'내용'  %><br />
    <%= f.text_area :content,:class=>'sl_input',:required=>'required',:cols=>100 %>
  </div>
  <div class="actions">
    <%= f.submit '제출' %>
  </div>
<% end %>